Run the following pip install command to install the required packages:

pip install -r requirements.txt

Run the following command to start the FastAPI server:

uvicorn main:app --reload --host 0.0.0.0 --port 8000


